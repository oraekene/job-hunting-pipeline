# 04 — Ticket conventions codified and conformed

**What to build:** the tracker doc codifies one status vocabulary (open, `ready-for-agent`, `claimed`, `blocked`, `resolved` — with `blocked` as the single word for "some done, rest needs the user"), `## Answer` as the required resolve section, and `[~]` as the partial-checkbox notation. The three resolved tickets in the compile-deploy effort gain answer sections and status lines from the documented set only.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] The tracker doc defines the vocabulary, resolve section, and partial-checkbox notation
- [ ] Each of the three resolved tickets has an `## Answer` section
- [ ] Grep over the feature directory shows no status word outside the documented set
- [ ] No `[x]` appears on any blocked or partial item in the effort's issues

**Agent brief:** A5 in `.scratch/review-gap-fixes/spec.md` (## Comments — Ticket conventions codified and conformed)